<div class="background">

  <div id="logo">
    <%= image_tag("logo/logo_big_white.png") %>
  </div>
  <% if user_signed_in? %>
    <button class= "cta button-cta" id="trigger-req">Start cooking</button>
  <% else %>
    <button class= "cta button-cta" id="trigger-req">Start cooking</button>
     <div class="signup-login">
      <a class="signup" href="/users/sign_up">Sign up</a>
      <a class="login" href="/users/sign_in">Log in</a>
    </div>
  <% end %>
  <p class='text-center'>" You don't have to cook fancy<br>
    or complicated masterpieces<br>
    just good food from fresh ingredients. "<br>
    <br>
    <span>Julia Child</span></p>
</div>

